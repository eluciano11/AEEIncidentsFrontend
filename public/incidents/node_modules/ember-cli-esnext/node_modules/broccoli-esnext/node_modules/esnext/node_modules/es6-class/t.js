var recast = require('recast');

var stdin = '';

process.stdin.on('data', function(chunk) {
  stdin += chunk;
});

process.stdin.on('end', function() {
  console.log(JSON.stringify(recast.parse(stdin).original, null, 2));
});
